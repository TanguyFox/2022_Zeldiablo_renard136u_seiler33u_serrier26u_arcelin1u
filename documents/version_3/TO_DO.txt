1. Transformer Perso en Interface qui serait implémenter par une classe abstraite Monstre et une classe concrète Joueur.

2. Dans Perso :

ajout f'un attribut List<Objet> inventaire;

methode boolean inventairePlein(){
    if(this.inventaire.size==5){
        return true;
    }
}

methode void recupererObjet(Objet objet){
    if(etrePresent(objet.getX(),objet.getY()){
        if(!inventairePlein){
            inventaire.add(objet);
        }
    }
}

labyDessin:

dessinerElement(Jeu jeu, Canvas canvas); OU une méthode par élément //pour dessiner les objets, les monstres et le perso;

Objet : methode boolean estPossedee(Perso p, Objet objet) //vérifie que l'objet objet se trouve dans l'inventaire de p;
        getObjet();
        setter x et y;

